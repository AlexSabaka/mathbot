metadata:
  id: k6_medium_integers_03
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-31
  grade: 6
  topic: arithmetic.integers
  family: integers
  difficulty: medium
  steps: 2
  tags: [integers, temperature, real_world, operations]

variables:
  city:
    type: location
  temp_start:
    type: integer
    min: 0
    max: 20
  temp_change1:
    type: integer
    min: -15
    max: -5
  temp_change2:
    type: integer
    min: -10
    max: 10
  Answer:
    type: integer
    format: temperature

template: |
  At sunrise in {{city}}, the temperature was {{temp_start}}°F. During the day, it changed by {{temp_change1}} degrees, and then by {{temp_change2}} degrees. What was the final temperature?

solution: |
  # Calculate final temperature with two changes
  temp_final = temp_start + temp_change1 + temp_change2
  Answer = temp_final

tests:
  - seed: 44444
    expected:
      answer: "3°F"
  
  - seed: 55555
    expected:
      answer: "7°F"
