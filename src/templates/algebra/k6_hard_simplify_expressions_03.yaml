metadata:
  id: k6_hard_simplify_expressions_03
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-31





















































      answer: "6y + 20"    expected:  - seed: 67890        answer: "6x + 32"    expected:  - seed: 12345tests:  Answer = f"{coefficient}{var} + {constant}"  constant = b * c + e  coefficient = a + b - d  # Combine: (a+b-d)*var + (b*c + e)  # Distribute: a*var + b*var + b*c - d*var + esolution: |  Simplify: {{a}}{{var}} + {{b}}({{var}} + {{c}}) - {{d}}{{var}} + {{e}}template: |    type: string  Answer:    max: 6    min: 2    type: integer  e:    max: 7    min: 1    type: integer  d:    max: 5    min: 2    type: integer  c:    max: 5    min: 2    type: integer  b:    max: 6    min: 3    type: integer  a:    choices: [x, y]    type: string  var:variables:  tags: [simplify, distributive_property, like_terms, complex]  steps: 3  difficulty: hard  family: simplify_expressions  topic: algebra.simplify  grade: 6  grade: 6
  topic: algebra.simplify
  family: simplify_expressions
  difficulty: hard
  steps: 3
  tags: [simplify, distributive_property, like_terms, complex]

variables:
  var:
    type: string
    choices: [x, y]
  a:
    type: integer
    min: 3
    max: 6
  b:
    type: integer
    min: 2
    max: 5
  c:
    type: integer
    min: 2
    max: 5
  d:
    type: integer
    min: 1
    max: 7
  e:
    type: integer
    min: 2
    max: 6
  Answer:
    type: string

template: |
  Simplify: {{a}}{{var}} + {{b}}({{var}} + {{c}}) - {{d}}{{var}} + {{e}}

solution: |
  # Distribute: a*var + b*var + b*c - d*var + e
  # Combine: (a+b-d)*var + (b*c + e)
  coefficient = a + b - d
  constant = b * c + e
  Answer = f"{coefficient}{var} + {constant}"

tests:
  - seed: 12345
    expected:
      answer: "6x + 32"
  
  - seed: 67890
    expected:
      answer: "6y + 20"
