metadata:
  id: k6_medium_lcm_03
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-31
  grade: 6
  topic: arithmetic.number_theory
  family: lcm
  difficulty: medium
  steps: 3
  tags: [lcm, multiples, real-world]

variables:
  a:
    type: integer
    min: 7
    max: 11
  b:
    type: integer
    min: 8
    max: 12
  Answer:
    type: integer

template: |
  Paper plates come in packs of {{a}}. Napkins come in packs of {{b}}. What is the least number of each item you need to buy to have the same number of plates and napkins?

solution: |
  # Calculate LCM
  lcm = abs(a * b) // math.gcd(a, b)
  Answer = lcm

tests:
  - seed: 33333
    expected:
      answer: "30"
  
  - seed: 44444
    expected:
      answer: "70"
