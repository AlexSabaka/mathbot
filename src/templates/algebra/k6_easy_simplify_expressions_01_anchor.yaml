metadata:
  id: k6_easy_simplify_expressions_01
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-31
  grade: 6
  topic: algebra.simplify
  family: simplify_expressions
  difficulty: easy
  steps: 1
  tags: [simplify, like_terms, addition]

variables:
  var:
    type: string
    choices: [x, y, n]
  a:
    type: integer
    min: 2
    max: 10
  b:
    type: integer
    min: 2
    max: 10
  Answer:
    type: string

template: |
  Simplify: {{a}}{{var}} + {{b}}{{var}}

solution: |
  # Combine like terms
  coefficient = a + b
  Answer = f"{coefficient}{var}"

tests:
  - seed: 12345
    expected:
      answer: "8y"
  - seed: 67890
    expected:
      answer: "12x"
