metadata:
  id: k6_medium_exponents_02
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-31
  grade: 6
  topic: algebra.exponents
  family: exponents
  difficulty: medium
  steps: 2
  tags: [exponents, powers, inverse_operations]

variables:
  base_exp_pair:
    type: string
    choices: ["2|4|16", "2|5|32", "2|6|64", "3|3|27", "3|4|81", "4|2|16", "4|3|64", "5|2|25", "5|3|125", "6|2|36"]
  Answer:
    type: string

template: |
  {% set parts = base_exp_pair.split('|') %}
  {% set base = parts[0] %}
  {% set exponent = parts[1] %}
  {% set result = parts[2] %}
  Express {{result}} as an exponential expression with base {{base}}.

solution: |
  # Parse the stored base_exp_pair
  parts = base_exp_pair.split('|')
  base = parts[0]
  exponent = parts[1]
  
  Answer = f"{base}^{exponent}"

tests:
  - seed: 22222
    expected:
      answer: "2^6"
  
  - seed: 33333
    expected:
      answer: "3^4"
