metadata:
  id: k6_easy_division_basic_01
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-31
  grade: 6
  topic: arithmetic.division
  family: division_basic
  difficulty: easy
  steps: 1
  tags: [division, basic_operations]

variables:
  divisor:
    type: integer
    min: 2
    max: 10
  quotient:
    type: integer
    min: 10
    max: 20
  Answer:
    type: integer

template: |
  Divide: {{divisor * quotient}} ÷ {{divisor}}

solution: |
  # Calculate the dividend first
  dividend = divisor * quotient
  Answer = dividend // divisor

tests:
  - seed: 12345
    expected:
      answer: "12"
  
  - seed: 67890
    expected:
      answer: "18"
