metadata:
  id: k6_medium_inequalities_02
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-31
  grade: 6
  topic: algebra.inequalities
  family: inequalities
  difficulty: medium
  steps: 2
  tags: [inequalities, solving, one_step]

variables:
  var:
    type: string
    choices: [x, n]
  a:
    type: integer
    min: 3
    max: 12
  b:
    type: integer
    min: 10
    max: 25
  Answer:
    type: string

template: |
  Solve and graph: {{var}} - {{a}} > {{b}}

  Describe the solution.

solution: |
  # Add a to both sides
  result = b + a
  Answer = f"{var} > {result}, meaning all numbers greater than {result}"

tests:
  - seed: 12345
    expected:
      answer: "n > 22, meaning all numbers greater than 22"
  - seed: 67890
    expected:
      answer: "x > 28, meaning all numbers greater than 28"
