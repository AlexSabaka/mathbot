metadata:
  id: k3_easy_perimeter_01
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-30
  grade: 3
  topic: geometry.measurement
  family: measurement
  difficulty: easy
  steps: 2
  culture: en-US
  tags: [perimeter, rectangle, measurement]

variables:
  length:
    type: integer
    min: 3
    max: 15
  width:
    type: integer
    min: 2
    max: 10
  unit:
    type: string
    choices: ["cm", "m", "inches", "feet"]
  Answer:
    type: integer
    format: length

template: |
  Find the perimeter of a rectangle with length {{length}} {{unit}} and width {{width}} {{unit}}.

  Please solve this problem.

solution: |
  # Perimeter = 2 × (length + width)
  Answer = 2 * (length + width)

tests:
  - seed: 12345
    expected:
      answer: "34 units"
  - seed: 67890
    expected:
      answer: "20 units"
