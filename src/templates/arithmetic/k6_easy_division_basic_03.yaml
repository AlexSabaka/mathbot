metadata:
  id: k6_easy_division_basic_03
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-31
  grade: 6
  topic: arithmetic.division
  family: division_basic
  difficulty: easy
  steps: 1
  tags: [division, basic_operations]

variables:
  divisor:
    type: integer
    min: 4
    max: 10
  quotient:
    type: integer
    min: 5
    max: 12
  Answer:
    type: integer

template: |
  What is {{divisor * quotient}} ÷ {{divisor}}?

solution: |
  # Calculate the dividend first
  dividend = divisor * quotient
  Answer = dividend // divisor

tests:
  - seed: 44444
    expected:
      answer: "9"
  
  - seed: 55555
    expected:
      answer: "8"
