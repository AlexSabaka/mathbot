metadata:
  id: k6_easy_factoring_03
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-31
  grade: 6
  topic: algebra.factoring
  family: factoring
  difficulty: easy
  steps: 1
  tags: [factoring, gcf, simple]

variables:
  var:
    type: string
    choices: [x, y, n]
  gcf:
    type: integer
    min: 3
    max: 8
  a:
    type: integer
    min: 2
    max: 6
  Answer:
    type: string

template: |
  Factor out the greatest common factor: {{gcf * a}}{{var}}

solution: |
  # GCF is gcf
  # Factor: gcf * a * var
  Answer = f"{gcf} * {a}{var}"

tests:
  - seed: 12345
    expected:
      answer: "6 * 5x"
  
  - seed: 67890
    expected:
      answer: "5 * 5n"
