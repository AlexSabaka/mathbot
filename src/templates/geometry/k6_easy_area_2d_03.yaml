metadata:
  id: k6_easy_area_2d_03
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-31
  grade: 6
  topic: geometry.area
  family: area_2d
  difficulty: easy
  culture: en-US
  steps: 2
  tags: [area, triangle, measurement]

variables:
  base_length:
    type: integer
    min: 10
    max: 18
  
  height_length:
    type: integer
    min: 8
    max: 16
  
  Answer:
    type: integer
    format: area

template: |
  A triangle has a base measuring {{base_length}} units and a height of {{height_length}} units.
  
  Calculate the area.

solution: |
  area = (base_length * height_length) / 2
  Answer = int(area)

tests:
  - seed: 33333
    expected:
      answer: "56 square units"
  
  - seed: 44444
    expected:
      answer: "72 square units"
