metadata:
  id: k6_medium_factoring_02
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-31
  grade: 6
  topic: algebra.factoring
  family: factoring
  difficulty: medium
  steps: 2
  tags: [factoring, gcf, variable_factor]

variables:
  var:
    type: string
    choices: [x, y, n]
  a:
    type: integer
    min: 4
    max: 10
  b:
    type: integer
    min: 2
    max: 8
  Answer:
    type: string

template: |
  Factor completely: {{a}}{{var}} - {{a * b}}

solution: |
  # GCF is a
  # Factor: a(var - b)
  Answer = f"{a}({var} - {b})"

tests:
  - seed: 12345
    expected:
      answer: "9(x - 7)"
  
  - seed: 67890
    expected:
      answer: "7(n - 6)"
