metadata:
  id: k6_medium_simplify_expressions_02
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-31
  grade: 6
  topic: algebra.simplify
  family: simplify_expressions
  difficulty: medium
  steps: 2
  tags: [simplify, like_terms, multiple_terms]

variables:
  var:
    type: string
    choices: [x, y, n]
  a:
    type: integer






























      answer: "6n"    expected:  - seed: 67890        answer: "7x"    expected:  - seed: 12345tests:  Answer = f"{coefficient}{var}"  coefficient = a - b + c  # Combine like termssolution: |  Simplify: {{a}}{{var}} - {{b}}{{var}} + {{c}}{{var}}template: |    type: string  Answer:    max: 6    min: 1    type: integer  c:    max: 7    min: 2    type: integer  b:    max: 9    min: 3    min: 3
    max: 9
  b:
    type: integer
    min: 2
    max: 7
  c:
    type: integer
    min: 1
    max: 6
  Answer:
    type: string

template: |
  Simplify: {{a}}{{var}} - {{b}}{{var}} + {{c}}{{var}}

solution: |
  # Combine like terms
  coefficient = a - b + c
  Answer = f"{coefficient}{var}"

tests:
  - seed: 12345
    expected:
      answer: "7x"
  
  - seed: 67890
    expected:
      answer: "6n"
