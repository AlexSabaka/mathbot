metadata:
  id: k6_easy_variables_02
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-31
  grade: 6
  topic: algebra.variables
  family: variables
  difficulty: easy
  steps: 1
  tags: [variables, evaluation, subtraction]

variables:
  var:
    type: string
    choices: [x, y, n, m]
  n:
    type: integer
    min: 10
    max: 30
  a:
    type: integer
    min: 3
    max: 15
  Answer:
    type: integer

template: |
  If {{var}} = {{n}}, what is the value of {{var}} - {{a}}?

solution: |
  # Evaluate expression with given variable value
  Answer = n - a

tests:
  - seed: 12345
    expected:
      answer: "14"
  
  - seed: 67890
    expected:
      answer: "8"
