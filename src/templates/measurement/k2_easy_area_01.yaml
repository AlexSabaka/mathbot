metadata:
  id: k2_easy_area_01
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-30
  grade: 2
  topic: measurement.area
  family: area
  difficulty: easy
  steps: 1
  culture: en-US
  tags: [area, counting, grid, squares]

variables:
  rows:
    type: integer
    min: 2
    max: 5
  cols:
    type: integer
    min: 2
    max: 5
  Answer:
    type: integer

template: |
  Count the square units to find the area of a rectangle that has {{rows}} rows and {{cols}} columns of squares.
  
  Please solve this problem.

solution: |
  # Area = rows × columns
  Answer = rows * cols

tests:
  - seed: 12345
    expected:
      answer: "12 square units"
  - seed: 67890
    expected:
      answer: "6 square units"
