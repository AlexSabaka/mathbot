metadata:
  id: k6_easy_equations_03
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-31
  grade: 6
  topic: algebra.equations
  family: equations
  difficulty: easy
  steps: 1
  tags: [equations, solving, one_step, multiplication]

variables:
  var:
    type: string
    choices: [x, y, n]
  a:
    type: integer
    min: 2
    max: 8
  product:
    type: integer
    min: 12
    max: 72
  Answer:
    type: integer

template: |
  Solve for {{var}}: {{a}}{{var}} = {{product}}

solution: |
  # Divide both sides by a
  Answer = product // a

tests:
  - seed: 12345
    expected:
      answer: "9"
  
  - seed: 67890
    expected:
      answer: "10"
