metadata:
  id: k6_easy_equivalent_expressions_03
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-31
  grade: 6
  topic: algebra.equivalent_expressions
  family: equivalent_expressions
  difficulty: easy
  steps: 1
  tags: [equivalent_expressions, constants]

variables:
  a:
    type: integer
    min: 2
    max: 12
  b:
    type: integer
    min: 2
    max: 12
  c:
    type: integer
    min: 5
    max: 24
  Answer:
    type: string

template: |
  Are these expressions equivalent? {{a}} + {{b}} and {{c}}
  
  Answer "yes" if they are equivalent, or "no" if they are not.

solution: |
  # Check if a + b equals c
  if a + b == c:
      Answer = "yes"
  else:
      Answer = "no"

tests:
  - seed: 12345
    expected:
      answer: "yes"
  
  - seed: 67890
    expected:
      answer: "no"
