metadata:
  id: k2_hard_addition_facts_03
  version: "1.0.0"
  author: Mathbot
  created: 2026-01-30
  grade: 2
  topic: arithmetic.addition
  family: addition
  difficulty: hard
  steps: 3
  culture: en-US
  tags: [addition, facts, three-addends, multiple-answers]

variables:
  num_a:
    type: integer
    min: 3
    max: 8
  num_b:
    type: integer
    min: 3
    max: 7
  num_c:
    type: integer
    min: 2
    max: 6
  Answer1:
    type: integer
  Answer2:
    type: integer

template: |
  Find two ways to add {{num_a}}, {{num_b}}, and {{num_c}}.
  
  First way: ({{num_a}} + {{num_b}}) + {{num_c}} = ___
  Second way: {{num_a}} + ({{num_b}} + {{num_c}}) = ___
  
  Please solve this problem.

solution: |
  # Both should give the same result (associative property)
  Answer1 = (num_a + num_b) + num_c
  Answer2 = num_a + (num_b + num_c)

tests:
  - seed: 56565
    expected:
      answer: "16 | 16"
  - seed: 67676
    expected:
      answer: "19 | 19"
